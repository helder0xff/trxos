
# This is an trxmake autogenerated file. Edit at your own risk.

include config.mk

# This module/project source, include and objects directories.
SRCDIR = ./../src
INCDIR = ./../inc
OBJDIR = ./obj

# Sources to compile.
SRCS := $(wildcard $(SRCDIR)/*.c)

# Include directories.
INCS = 	-I$(INCDIR)
INCS += -I$(ROOT)/mods/$(MOD1)/inc/

# Objects to be build/linked.
OBJS := $(SRCS:.c=.o)

# Preprocessor and assembly rules; mainly used for debugging purposes.
PREPROS := $(SRCS:.c=.i)
ASMS := $(SRCS:.c=.asm)
